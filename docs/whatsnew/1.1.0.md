- Major internal refactor into modular packages (`app/`, `models/`, `services/`, `ui/`)
- New architecture doc (`docs/architecture.md`)
- Live Code Preview: **Comment Selection** (`Cmd/Ctrl+/` and Edit menu)
- Updated keyboard shortcut docs
- Multi-window tabs (add/rename/remove) with at least one window required
- `.pfb` now saves all windows and active tab; old `.pfb` files auto-migrate on load
- Window naming now generates `create_<window_name>` methods and `self.<window_name>` objects
- Double-click a window tab to rename it
- Deleting the last remaining window now shows a warning message
- Generate Script now asks which class to update when multiple classes are detected in edited preview code
- New Analyze Existing Script tool (read-only) lists classes and detected `create_*` methods
- Added Uncomment Selection in Edit menu; Cmd/Ctrl+/ now toggles comment state
- Added +/− buttons at top-right of tabs for quick window add/remove (remove disabled with one window)
- Added Import Existing Script (beta) for class-based `create_*` window tab import
- Beta import now pulls deterministic widget/layout patterns into editable tabs
- Beta import now resolves simple constants and shows skipped-item reasons
- Beta import now better normalizes enum/bool/int/list property values
